package com.sena.app_backend.model;

/**
 * Esta clase representa los tipos de usuarios que pueden existir en la aplicación.
 */
public enum Rol {
  USUARIO,      // usuario normal de la plataforma
  ADMINISTRADOR // tiene privilegios de administración
}
